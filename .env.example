# MongoDB Configuration
MONGO_URI=mongodb://localhost:27017/printmaster

# Session Configuration
SESSION_SECRET=your-super-secret-key-change-this-in-production
SESSION_NAME=printmaster.sid

# Server Configuration
PORT=3001
NODE_ENV=development

# Frontend URL (para enlaces en emails)
FRONTEND_URL=http://localhost:5173

# Email Configuration (SMTP)
# Opción 1: SMTP tradicional (Gmail, Outlook, etc.)
# Para Gmail: smtp.gmail.com, puerto 587 o 465 (SSL)
# Para Outlook: smtp-mail.outlook.com, puerto 587
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Opción 2: Resend.com (API moderna, recomendado)
# Obtén tu API key en: https://resend.com/api-keys
# Dominio por defecto (sandbox): onboarding@resend.dev
# Dominio verificado: tu-dominio.com (verificar en: https://resend.com/domains)
EMAIL_PROVIDER=resend
RESEND_API_KEY=re_U4CaEsSQ_9KoATe9phd34E6nbgnSrD9Uy

# Email Sender Info
EMAIL_FROM_NAME=PrintMaster System
EMAIL_FROM_ADDRESS=onboarding@resend.dev

# SNMP Configuration (opcional)
SNMP_TIMEOUT=5000
SNMP_RETRIES=1
